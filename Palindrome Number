class Solution {
    public boolean isPalindrome(int x) {
    int r=0;
    int sum=0;
    int temp=x;
    while(temp>0){
        r=temp%10;
        sum=sum*10+r;
        temp/=10;
    }
    if(x==sum){
        return true;
    }
    else{
        return false;
    }
  }
}
